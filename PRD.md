# PRD — 초축 리그 MMR 시스템 (간소화 버전)

작성일: 2026-07-13 (rev.6 — **다중 리그 개정**: 리그 생성 마법사 도입, 시드 명단 제거)
근거 자료: `표본/` 폴더 — **리그기획안.txt**(랭크 규칙), **초축인티어표.txt**(티어 배치 참고 예시)

> **rev.6 개정 취지**: 비개발자 관리자가 웹에서 직접 **리그를 생성**하는 구조로 전환한다.
> 첫 접속 시 리그가 없으면 온보딩 마법사가 뜨고(§3.0), 리그는 시즌마다 여러 개 만들 수 있으며
> 과거 리그도 선택해 조회할 수 있다. 초축인티어표 45명을 시드 데이터로 심는 전제는 **삭제** —
> 모든 리그는 빈 상태에서 마법사로 세팅한다 (부록 A는 입력 참고용으로만 보존).
>
> **rev.5 개정 취지 (유지)**: 스탯 8종 입력·육각형 스텟·구 Elo 병행 표시·스킬 통계는
> 입력 부담이 커서 전부 **제외** (→ §9 제외 기능). 시스템은 **"경기 결과(승/무/패) 입력 → MMR 자동 계산·조회"**
> 한 가지에 집중한다. 제외 기능의 상세 스펙이 다시 필요해지면 `PRD-rev4.md`(보관본)를 참조.

---

## 1. 개요

스타크래프트 유즈맵 "초능력축구" 리그의 MMR 관리 웹 시스템.
관리자가 경기 하나당 **출전 선수 8명(4:4)과 승/무/패**만 입력하면 MMR·랭킹·팀 순위가 즉시 자동 반영되고,
참가자는 누구나 브라우저로 조회한다 (로그인 불필요).

**리그 단위 운영**: 시스템은 리그(=시즌)를 여러 개 담는 그릇이다. 선수·팀·경기·규칙 설정은 전부
**리그 소속**이며, 시즌마다 새 리그를 생성하고 과거 리그는 보관 상태로 계속 조회할 수 있다.
첫 접속 시(리그 0개) 온보딩 마법사가 자동으로 떠서 비개발자 관리자도 안내만 따라가면 리그 개설이 끝난다 (§3.0).

| 역할 | 행동 |
|---|---|
| 관리자 (1~2명) | 리그 생성·보관, 경기 입력·수정·삭제, 선수/팀/티어 관리, 주간 갱신 확인 |
| 참가자 (수십 명) | 리그 선택 → MMR 랭킹·팀 순위·본인 MMR 추이 조회 |

**첫 리그(예정)**: 2026 썸머리그 (팀 4개 내외, 감독·코치 지정)

## 2. 목표 / 비목표

### 목표
1. **비개발자 셀프 서비스**: 온보딩 마법사로 리그 생성부터 운영까지 개발자 개입 없이 가능
2. 경기 입력 최소화: **날짜·유형·출전 4:4·결과(승/무/패)** — 끝
3. 기획안 랭크 시스템 정확 구현: 티어별 MMR, ±30p, 언더독, 월요일 갱신, 승급/강등, 휴면 강등
4. 경기 수정/삭제 시 전체 MMR 자동 재계산 (엑셀 대비 핵심 개선)
5. 저사양 상시 구동, 외부 인터넷 공개 (도메인 + HTTPS)

### 비목표 (이번 버전에서 하지 않음 — §9)
- 개인 스탯(골/어시/터치 등) 입력·통계 일체
- 육각형 스텟 차트, 구 Elo 레이팅, 스킬 관리/통계, 엑셀 과거 기록 임포트

## 3. 핵심 기능

### 3.0 리그 생성기 (리그 목록 + 셋업 체크리스트)

**첫 접속 시 리그가 하나도 없으면 자동으로 생성 흐름 진입.** 개발자 없이 관리자 혼자 리그를 개설한다.
기본 화면은 **리그 목록** — 리그를 만들면 카드로 뜨고, 리그를 선택하면 **셋업 체크리스트**가 표시된다.
각 항목은 완료 여부(✓)가 표시되고 **순서와 무관하게 아무 항목이나 진입해 수정** 가능:

> 리그 정보 → 선수 등록 → 티어 배치 → 스킬 티어 **[여기까지 = 리그 생성 완료]** → 팀 편성 → 드래프트

1. **리그 정보**: 리그 이름(예: 2026 썸머리그) + **기간(시작일~종료일)**.
   **기간은 리그 진행 중에도 수정 가능** — 주차·월요일 갱신은 기간에서 자동 재산출. 관리자 비밀번호는 시스템 최초 1회만
2. **선수 등록**: 전역 **유저 풀**(§3.1)에서 참가자를 선택해 리그에 등록. 없는 사람은 이름만으로 즉석 "유저 추가"
   (여러 줄 붙여넣기 일괄 등록 지원). **시드 명단 없음 — 빈 상태에서 시작**
3. **티어 배치 보드** (§3.1): 등록한 선수를 **드래그해서 티어(S~C-)에 배치** — 티어가 곧 급여·초기 MMR을 결정.
   티어별 **급여·초기 MMR·정원**을 이 화면에서 리그마다 직접 설정 (기본값 = 기획안 수치)
4. **스킬 티어 배치 보드** (§3.1): 스킬 54종은 **고정 마스터로 자동 등록**(리그와 무관하게 불변) —
   리그마다 달라지는 스킬 티어(S/A/B/C)만 드래그로 배치해 확정. 기본값(부록 B)으로 미리 채워져 있음
5. **팀 편성**: **팀명 + 감독**만으로 팀 생성(코치는 선택), 최대 4팀 권장 → 십자 보드(§3.1)에서 로스터 관리
6. **드래프트 — 경매** (§3.5): 티어별 획득 상한·라운드 풀·코인을 설정하고 경매 진행. 경매 없이 직접 구성해도 됨

- 모든 항목은 나중에 다시 진입해 수정 가능 — 체크리스트는 진행 안내일 뿐 강제 순서가 아님
- 규칙 상수(±30p, 언더독, 급여 상한 등)는 기본값 자동 세팅 — 마법사에서 묻지 않고 설정 화면에서만 변경
- **추가 리그 생성**: 관리자 메뉴 "새 리그 만들기" → 동일 마법사 재사용. **기존 리그에서 선수 명단 복사** 옵션 제공(이름만 가져오고 티어는 재배치, MMR·전적은 리셋)
- **리그 상태**: 진행 중(active) / 보관(archived). 보관 리그는 조회 전용 — 경기 입력·갱신 불가. 진행 중 리그는 동시에 여러 개 가능하되 기본 노출은 최신 리그

### 3.1 마스터 데이터
- **유저 (전역)**: 시스템 공통 명단 — **이름만으로 등록**("유저 추가"). 리그와 무관하게 유지.
  닉네임·리그 통산 이력 연동은 로드맵 (유저 1명 ↔ 여러 리그의 참가 기록)
- **선수 (리그 참가)**: 유저를 리그에 등록한 것 — 소속 팀, 티어(S~C- 10단계), 최초 배치 티어(승격 보상 판정용),
  **주 포지션**(공격/미드/수비 — 선택지 목록은 설정 가능), MMR은 전부 **리그별** 값
- **티어 정원** (설정 가능): S 8 / A+ 4 / A 3 / A- 1 / B+·B·B-·C+·C 각 6 / **C- 무제한**(최하위 받이) —
  이 정원이 곧 주간 갱신의 **순위 재배치 구간**이다 (§3.3)
- **티어 배치 보드** (관리자 — 마법사 4단계 겸 티어 관리 화면):
  - 티어별 가로 행(S~C-)에 선수를 **드래그해서 배치/이동**, 사이드바에 미배치 선수 목록
  - 행마다 **급여·스타트 MMR·정원 입력** — 리그별 `tier` 테이블 값 (C-는 정원 없음)
  - 현재 인원/정원 실시간 표시, 정원 초과 시 경고 (저장은 허용 — 운영 예외 대비)
  - **정원 합계 검증 (중요)**: C- 제외 정원 합계가 배치 인원보다 크면 강한 경고 —
    "첫 주간 갱신 때 하위 티어가 비면서 전원이 상향 배치됩니다. 정원을 실제 인원에 맞추세요"
    (정원 = 순위 재배치 구간이므로, 리그 인원에 맞게 정원을 조정하는 것이 운영 전제)
- **스킬 마스터 (고정)**: 54종(부록 B)의 이름·아이콘 — 리그와 무관하게 불변, 시스템 내장 시드.
  아이콘은 스킬별 이모지 기본 제공, `public/icons/<스킬명>.png` 파일이 있으면 자동으로 이미지 표시
- **스킬 티어 배치 보드** (마법사 5단계 겸 스킬 관리 화면):
  - S/A/B/C 4행에 스킬 칩(아이콘+이름)을 **드래그해서 리그별 티어 확정**
  - 기본값 = 부록 B 배치, "기본값 복원" 버튼 제공
  - 행마다 팀 소유 상한(기본 S 2 / A 4 / B 4 / C 4 — 기획안 3-3·4-3, **리그별 수정 가능**)·현재 스킬 수 표시 (스킬 뽑기는 현장 진행 — 시스템은 티어표 관리만)
- **팀**: 팀명, **감독, 코치**, 로스터, **보유 코인**, **소유 스킬**. 제약(위반 시 경고, 저장은 허용 — **수치는 모두 리그별 설정값**):
  - 총원 최대 9명 · S~A- 최대 4명 · B+~C- 최대 7명 (기본값 — 팀 편성 보드에서 리그마다 수정)
- **팀 코인**: 드래프트 잔여 코인이 시즌 중에도 팀 자산으로 유지 — 관리자가 언제든 **지급/차감**(사유 기록, `coin_log` 이력).
  리그 운영 보상·이벤트 지급 용도
- **팀 벤/리로드 게이지** (기획안 4-3·5장): 팀별 정수 카운터 — **게이지 `gaugePerUse`개(기본 5)당 벤/리로드 1회 사용권**(누적 가능).
  코인과 동일하게 관리자가 **지급/차감**하고 원장(`type: "gauge"`)에 기록되며, 항목 취소 시 역기록.
  획득처(주간 리그 순위 1위 1개~4위 4개 · 임대 경기당 1개 · 인시즌 토너먼트 우승 5/준우승 2/4강 1 · 카페 보상)는
  **관리자가 판단해 수동 지급** — 자동 지급은 미구현(§9-1)
- **팀 스킬 소유**: 스킬 뽑기(현장 진행) 결과를 관리자가 등록 — 스킬 하나의 소유 팀은 1곳.
  팀당 총 **13개**(설정 가능) + 티어별 상한(스킬 티어 보드 값) 초과 시 경고.
  팀 편성 보드·공개 팀 순위표에 아이콘으로 표시
- **스킬 구매 (시즌 중)**: 팀 코인으로 미소유 스킬 구매 — **티어별 가격**(설정 가능, 기본 S 50 / A 30 / B 20 / C 10,
  스킬 티어 보드에서 수정). 구매 시 코인 차감(`coin_log` 기록), 잔여 코인 부족 시 구매 불가. 소유 해제 시 환불 없음(기본)
- **트레이드 (시즌 중)**: 두 팀 간 선수 교환 — 각 팀에서 넘길 선수 선택(한쪽만 넘기는 편도 이적도 가능) +
  **코인 차액** 포함 가능. 실행 시 소속 교체·코인 이동·`trade_log` 기록, 로스터 제약 위반 시 경고. 팀 편성 보드에서 실행
- **방출·영입 (시즌 중)**: 로스터 정원(총 9명 — 감독 포함, 설정값) 확보를 위해 선수 **방출**(팀 → 무소속)
  또는 무소속 선수 **영입**. 모든 로스터 이동(방출/영입/이적/트레이드)은 `roster_log`로 기록 → 거래 내역에 표시
- **팀 편성 보드** (관리자 — 마법사 4단계와 리그 개설 후 로스터 관리 겸용):
  - 화면을 **십자로 4분할**해 팀당 한 칸 (팀이 2~3개면 분할 수 자동 조정)
  - 사이드바에 **미배정 선수 목록** — 스크롤, 티어 뱃지·급여 표시, 이름/티어 검색
  - 사이드바 → 팀 칸으로 **드래그 앤 드롭**하면 해당 팀 소속으로 저장. 팀 간 이동, 사이드바로 되돌리기(방출)도 드래그로
  - 각 팀 칸에 인원 수·급여 합·제약 위반 경고 실시간 표시 — **제약 수치(총원/S~A-/B+~C-)는 보드 상단에서 리그별 수정**
  - 팀 칸에서 **보유 코인·벤/리로드 게이지 직접 수정**(지급/차감) 및 **소유 스킬 등록/해제** — 스킬 수 n/13, 티어 상한 초과 경고
  - **급여는 표시하지 않는다** — 급여는 출전 4명의 합에만 의미가 있는 값이므로(기획안 4-1) 팀 전원 급여 합·상한은
    이 화면에서 제외하고, 급여 상한과 **팀별 상한 보너스(`cap_bonus`)** 입력은 경기 입력 화면에 둔다
  - **팀 생성/삭제도 이 보드에서**: [+ 팀 만들기] → 빈 칸 생성, 팀명·감독은 칸 머리글에서 인라인 수정.
    팀 삭제 시 소속 선수는 미배정으로 복귀. 보드 분할은 팀 수(1~4)에 맞춰 자동 조정
  - 모바일 폴백: 드래그 대신 선수 탭 → 팀 선택

### 3.2 경기 입력 (관리자)
한 화면에서 경기 1건:

- 날짜, 주차, 유형(**리그 / 아마추어 / 프로**), 당일 순번
- 레드/블루: 팀 선택(리그) 또는 임시 팀명, **선수 4명씩**, **결과(레드 승 / 블루 승 / 무)**
- 팀별 **사용 스킬 입력**: 그 팀이 **소유한 스킬만** 선택지에 노출(§3.1 팀 스킬 소유) —
  같은 날짜에 그 팀이 이미 사용한 스킬을 다시 고르면 **피어리스룰 경고**.
  전·후반 구분 없이 경기 단위 통합 기록(의도된 단순화).
  **아마추어/프로 경기는 스킬 자유 사용**(기획안 8-1) — 소유 제한·피어리스룰 미적용, 전체 스킬에서 선택
- 저장 전 자동 경고: 급여 합 > 상한(기본 90 + 팀별 보너스) ·
  **아마추어 = B·B- ·C+·C·C- / 프로 = S·A+·A·A-·B+** 자격 위반 (기획안 8-1·8-2 — 두 구간이 10개 티어를 양분,
  `engine.js`의 `AMA_TIERS`/`PRO_TIERS` 단일 출처) ·
  **소속팀 없는 선수의 아마추어/프로 참여**(기획안 8-1) · 중복 출전 · 4명 미달
  — 급여 초과 골 패널티(-5골/5점)는 **게임 내에서 적용**하는 규칙이므로 시스템은 경고만 표시
- 저장 즉시 MMR 반영. **수정/삭제 시 이후 전체 MMR 자동 재계산**

### 3.3 MMR 엔진

#### 티어별 초기 MMR / 급여 (설정 가능)

| 티어 | MMR | 급여 | | 티어 | MMR | 급여 |
|---|---|---|---|---|---|---|
| S | 3300 | 30 | | B+ | 2700 | 20 |
| A+ | 3100 | 25 | | B | 2600 | 20 |
| A | 3000 | 25 | | B- | 2500 | 20 |
| A- | 2900 | 25 | | C+ | 2300 | 15 |
| | | | | C | 2200 | 15 |
| | | | | C- | 2100 | 15 |

#### 경기 반영 (리그/아마추어/프로 동일)
- 기본: **승 +30p / 패 -30p / 무 0p**
- **언더독** — 출전 4인 MMR 합이 **500p 이상 차이**:
  - 약팀 승리 **+40** / 패배 **-20** · 강팀 승리 **+20** / 패배 **-40**

#### 주간 갱신 (매주 월요일, M2)
- 경기별 MMR 변동은 즉시 계산·표시하되, **공식 순위·티어 재배치·급여는 월요일 배치로 확정**
- **승급/강등 = MMR 순위 기반 재배치 (확정 2026-07-14)** — 티어별 MMR "기준값"은 없다.
  티어 배치 때 받는 MMR은 **스타트 값일 뿐**이며, 매주 월요일 활성 선수 전체를 MMR 내림차순으로 정렬해
  **티어 정원대로 위에서부터 채운다**:
  `S(8) → A+(4) → A(3) → A-(1) → B+(6) → B(6) → B-(6) → C+(6) → C(6) → C-(나머지 전원, 정원 없음)`
  — 정원은 리그별 설정값(티어 배치 보드와 동일 수치)
  - 유지 구간·정원 스왑·"S 3200 + TOP 8" 조건이 전부 자연 해결: 누군가 오르면 정원에 밀려 누군가 내려간다
  - MMR 동점 타이브레이커 (기본값, 설정 가능): 승수 → 승률 → 이름 가나다순
- **갱신 순서**: ① 휴면 강등 -200p 적용 → ② MMR 순위 산정 → ③ 티어 재배치(`tier_history` 기록) → ④ 급여·승격 보상 갱신
- **휴면 강등**: 1~3주차 미참여 -200p, 4~5주차 미참여 -200p (각 1회, 경기 기록에서 자동 산출, `mmr_adjust`로 기록)
  - 판정 시점: 1~3주차분은 **4주차 월요일**, 4~5주차분은 **시즌 종료 갱신**에서
  - 시즌 중간 등록 선수는 **등록 이전 주차 면제**
  - '참여' 인정 범위 (확정 2026-07-14): **리그/아마추어/프로 모든 유형 출전 인정**
- **승격 보상 (확정 2026-07-14 — 육성 보상, 회수 없음)**: 주간 갱신에서 선수가
  **최초 배치 티어보다 높은 티어로 처음 올라간 순간**, 그 시점의 소속 팀에 승격 카운트 +1 (**선수당 평생 1회**, 무소속이면 적립 없음).
  카운트 2개당 팀 급여 상한 +5, 최대 +15 (기획안: 90→95→100→105).
  **한 번 적립되면 이후 강등·트레이드·방출과 무관하게 팀에 유지** — 트레이드로 이미 승격한 선수를 데려와도 적립 없음
  (승격 직전 선수 사재기로 보상 파밍 불가)
- 앱 내장 스케줄러 + 수동 실행 버튼, 서버가 꺼져 있었으면 재기동 시 자동 보정

### 3.4 조회 (공개 페이지)
- **공개는 조회 전용, TOP RATE 중심** — 경기 입력·수정, 셋업, 로그 관리 등 운영 기능은 전부
  관리자 페이지(로그인)로 분리. 셋업 체크리스트도 관리자 화면에만 표시
- **리그 선택**: 상단 드롭다운으로 리그 전환 (기본 = 가장 최근 진행 중 리그). 아래 모든 페이지는 선택된 리그 기준
- **MMR 랭킹 (TOP RATE)** — **공개 기본 화면**: 티어 뱃지, **소속 팀·주 포지션**, MMR, 주간 변동 ▲▼, 전적(승-패-무), S 유지선 표시
- **팀 순위표** (공개 — TOP RATE 옆 탭): 승/무/패, **승점(승×3+무)**, 감독·코치,
  **로스터 전원(티어 뱃지·포지션) 공개**, 소유 스킬(아이콘), 보유 코인(공개 — 확정 2026-07-14),
  **벤/리로드 게이지**(잔량 + 사용 가능 횟수 — 스킬 기능이 켜진 리그만)
  - 집계는 **리그 유형 경기만** (아마추어/프로 임시 팀 경기는 제외)
  - 동률 타이브레이커: **승점 → 다승 → 승자승 → 팀명 가나다** (설정 가능)
- **경기 로그**: 목록/상세 — 라인업, MMR 변동 ±, 언더독 표시. 유형/주차 필터
- **선수 페이지** (이름 클릭 진입): 현재 MMR·티어, 전적·승률, **MMR 추이 그래프**, 티어 변동 이력, 최근 경기

### 3.5 드래프트 — 경매 (체크리스트 '드래프트' 항목, 언제든 재진입 가능)

리그 시작 전 팀 로스터를 **코인 경매**로 구성한다. 실제 경매(음성/현장)는 밖에서 진행하고,
시스템은 진행 순서 관리와 결과 기록을 맡는 도구.

- **설정** (경매를 열 때 확정 — 시작 후 잠금):
  - 라운드 수 (기본 2)
  - **라운드별 팀 지급 코인** — 1라운드 지급량 / 2라운드 추가 지급량 각각 입력 (팀 전체 동일 지급).
    기본값 **1R 200 / 2R 200** (기획안 3-2 — 2라운드는 남은 돈을 회수하지 않고 200을 추가 지급)
  - 잔여 코인 이월: 1라운드에서 남은 코인은 2라운드로 이월 (기본 켜짐, 설정 가능)
  - **팀당 티어별 획득 상한** — 한 팀이 경매로 데려갈 수 있는 인원을 티어 구간별로 제한.
    기본: **S급 2명 / A급 2명 / B급 6명 / C급 6명** (수치는 리그별 설정).
    카운트 = **현재 로스터 기준, 감독만 제외** (경매 전에 배정돼 있던 선수 포함)
  - 경매 열기 전 검증: 풀 인원 > 전 팀 상한 총 슬롯이면 경고 (다 팔릴 수 없는 상태)
- **라운드 풀 배정 — 드래그 앤 드롭**: 경매를 열기 전 **1라운드 / 2라운드 / 경매 제외** 3열 보드에서
  선수 칩을 끌어서 넣고 뺀다 (기본 배정: S~A- → 1R, B+~C- → 2R). 배정된 풀 명단이 경매 화면 대기 목록에 그대로 노출.
  감독은 자기 팀 자동 소속 — 풀에서 자동 제외. 제외 선수는 이후 편성 보드에서 수동 배정
- **진행 화면**: 라운드 풀 **전체 명단이 대기 목록으로 노출**되고, 거기서 **랜덤으로 1명씩 지명**(추첨 연출) → 감독들이 현장에서 호가 → 결과 입력
  - **낙찰**: 낙찰 팀 선택 + 낙찰 코인 입력 → 그 팀 소속 + 코인 차감. **잔여 코인 초과 입력 차단**,
    **티어 획득 상한에 도달한 팀은 해당 선수 입찰 불가(버튼 비활성)**
  - **유찰**: 풀 맨 뒤로 — 같은 라운드 안에서 다시 등장
  - **교착 방지**: 지명된 선수를 **모든 팀이 상한 때문에 입찰 불가**하면 자동으로 '경매 제외' 처리
    (무한 유찰 방지) → 종료 후 편성 보드에서 수동 배정
  - 팀별 잔여 코인·**티어 구간별 획득 수(상한 대비)** 실시간 표시, 로스터 제약(§3.1) 위반 시 경고
- 라운드 풀 소진 → 다음 라운드 시작 (추가 코인 지급)
- 전 과정 이력 저장 (`draft_result`: 등장 순서·선수·낙찰 팀·낙찰가) — 되돌리기(직전 낙찰 취소) 지원
- 종료 후 팀 편성 보드에서 자유롭게 수동 조정 가능

### 3.6 관리
- 단일 관리자 로그인 (비밀번호는 마법사에서 최초 설정, 이후 설정에서 변경) — 모든 리그 공통 계정
- **거래 내역 로그** (관리자 — 경기 관리 화면): 코인·로스터가 움직인 모든 사건의 통합 타임라인 —
  관리자 지급/차감 · 드래프트 낙찰 · 스킬 구매 · 트레이드 · 방출/영입/이적 (`coin_log`+`trade_log`+`roster_log`), 팀별 필터
  - **항목 취소** 지원: 오입력 복구용 — 원본을 지우지 않고 **역기록을 추가**하는 방식 (원장 무결성 유지)
- 리그 관리: 새 리그 만들기(마법사), 리그 이름/시작일 수정, 보관 처리
- 규칙 상수 전부 **리그별 설정값** (포인트, 언더독 기준/보정, 급여, 정원 …) — 리그 생성 시 기본값 복사
- 백업 = `data/` 폴더 복사 (JSON — §6 저장 방식) + 자동 스냅샷 20개 보관 + "백업 다운로드" 버튼

## 4. 비기능 요구사항

| 항목 | 요구 |
|---|---|
| 스택 | Node.js 내장 기능만 (node:http) — **외부 패키지 0개**. 저장은 **JSON 파일** (§6 저장 방식) |
| 배포 | Windows 백그라운드(`시작.bat`) / Linux 서버 동일 코드. 외부 공개 시 리버스 프록시 + HTTPS |
| 규모 | 선수 ~100명, 시즌당 경기 수백 건 — 메모리 상주, 전체 재계산 1초 이내 |
| 보안 | 관리자 로그인 시도 제한(레이트리밋) + 세션 토큰. 조회는 비로그인 공개 |
| UI | 한국어, 모바일 대응, 다크 e스포츠 디자인 (§5) |

## 5. 디자인 가이드 (확정 2026-07-12, 목업 승인)
- 다크 네이비/블랙, op.gg류 전적 사이트 톤. Pretendard(셀프 호스팅)
- 티어 뱃지: S 금색 / A 보라 / B 파랑 / C 회색, 세부 티어는 명도 차. 전 화면 동일 컴포넌트
- MMR 상승 ▲초록 / 하락 ▼빨강, 랭킹표에 승급/강등 구분선
- 그래프(MMR 추이)는 경량 SVG 직접 렌더링, 차트 라이브러리 미사용

## 6. 데이터 모델

### 저장 방식 — JSON 파일 (확정 2026-07-14, SQLite에서 변경)
- `data/system.json` — 관리자 계정, 전역 유저 풀
- `data/league-<id>.json` — 리그 하나의 전체 상태 (설정·선수·팀·스킬 티어·드래프트·경기·원장·갱신 이력)
  — 리그별 파일 분리로 보관(archive)·백업·수동 확인이 파일 단위로 가능
- **쓰기 규칙**: 메모리 상주 → 변경 시 **임시 파일에 쓰고 rename(원자적 교체)** — 저장 중 프로세스가 죽어도 직전 파일 무손상
- **자동 백업**: 저장 시점 스냅샷을 `data/backups/`에 최근 20개(설정 가능) 보관 + 관리자 "백업 다운로드" 버튼
- 마이그레이션: 기존 `league.db`(SQLite) → JSON 1회 변환 스크립트
- 아래 스키마는 **논리 구조** — JSON 키 구조로 구현한다

```
user(id, name UNIQUE, nickname, created_at)                -- 전역 유저 풀 (닉네임·통산 이력 연동은 로드맵)
league(id, name, start_date, end_date, status, created_at) -- status: active | archived. 기간은 진행 중 수정 가능(주차 자동 재산출)
admin(password_hash)                                       -- 시스템 공통 (리그 무관)
config(league_id, key, value)                              -- 규칙 상수 전부 (리그별)
tier(league_id, code, ord, mmr_initial, salary, capacity)  -- mmr_initial = 스타트 MMR(기준값 아님); C-는 capacity NULL(무제한)
team(id, league_id, name, manager_player_id, coach, cap_bonus, coins, active)
    -- UNIQUE(league_id, name); coins = 보유 코인(시즌 중 유지)
    -- manager_player_id = 감독(로스터 선수 지정, 총원·경매 제외 판정 기준) — 감독을 방출/트레이드하면 지정 해제
    -- 경기 기록이 있는 팀은 삭제 대신 active=false (과거 기록 보존)
team_skill(team_id, skill_id)                              -- 팀 스킬 소유 (스킬당 소유 팀 1곳)
coin_log(id, team_id, delta, reason, at)                   -- 코인 지급/차감 이력 (드래프트 낙찰·스킬 구매 포함)
trade_log(id, league_id, at, team_a, team_b, players_ab, players_ba, coins_ab)   -- 트레이드 이력
roster_log(id, league_id, player_id, from_team, to_team, at)   -- 방출(to NULL)/영입(from NULL)/이적 이력
league_player(id, league_id, user_id, team_id, tier, initial_tier, position, mmr_live, mmr_official, promo_credited, active)
    -- UNIQUE(league_id, user_id); mmr_official = 지난 월요일 확정값(주간 변동 ▲▼ 기준)
    -- promo_credited = 승격 보상 적립 완료 플래그(선수당 평생 1회) — 적립 시점 소속 팀의 cap_bonus에 반영
    -- active=false(탈퇴/제외): 랭킹·순위 재배치에서 제외, 기록은 유지
mmr_adjust(id, league_id, player_id, delta, reason, at)
    -- 경기 외 MMR 변동 이벤트: 휴면 강등 -200p, 관리자 수동 보정 — 재계산 시 경기와 시간순으로 함께 리플레이
skill(id, name, icon)                                      -- 고정 시드 54종 (리그 무관, 불변)
skill_tier(league_id, skill_id, tier)                      -- 리그별 스킬 티어 (기본값 = 부록 B)
draft_pool(league_id, player_id, round)                    -- 경매 라운드 풀 배정 (없으면 경매 제외)
draft_result(id, league_id, round, seq, player_id, team_id, price)   -- 등장 순서·낙찰 이력 (유찰 재등장 포함)
match(id, league_id, type, week, played_at, day_seq,
      red_team_id, blue_team_id, red_name, blue_name,
      result,                         -- red | blue | draw (입력값)
      underdog_side)                  -- 파생
match_player(match_id, player_id, side,
      mmr_before, mmr_delta, mmr_after)   -- 파생(재계산 시 갱신)
match_skill(match_id, side, skill_id)     -- 경기에서 사용한 스킬 (팀 소유 스킬만 선택 가능)
tier_history(id, player_id, from_tier, to_tier, reason, at)   -- M2
weekly_update(id, league_id, week, ran_at, summary)           -- M2
```

- `week`는 `league.start_date` 기준 자동 계산(수동 보정 가능) — **시작일이 속한 주(월요일 시작)가 1주차**
- 파생 값은 저장하되 원본에서 언제든 전체 재계산 가능 (재계산은 리그 단위) —
  **재계산 = 경기 + `mmr_adjust` 이벤트를 시간순 리플레이** (휴면 페널티·수동 보정 증발 방지),
  언더독 판정도 재계산 시 다시 산출
- **재계산 소급 정책**: 재계산은 MMR 등 파생값만 갱신 — **이미 확정된 주간 티어 재배치는 소급하지 않는다**
  (다음 월요일 갱신에 자연 반영). 필요 시 관리자가 **"이번 주 갱신 다시 실행"**(직전 갱신 덮어쓰기, 이력 유지)
- ※ rev.4의 `red_score/blue_score`·스탯 컬럼은 입력에서 제외 — 기존 DB 컬럼은 남겨두되 사용하지 않음.
  기존 단일 리그 DB는 마이그레이션 시 "리그 1개"로 감싸서 이전

## 7. 화면 구성
0. **관리자 — 리그 목록 + 셋업 체크리스트 허브** (§3.0) — 리그 카드(이름·기간 진행 중 수정), 체크리스트에서 각 셋업 화면으로 이동
   (티어 배치 · 스킬 티어 · 팀 편성 보드 · 경매 드래프트). 체크리스트는 관리자 화면에만 표시
0-1. **관리자 — 유저 관리** — 전역 유저 추가/목록, 현재 리그 참가 등록, 주 포지션 지정
0-2. **관리자 — 경기 관리** — 경기 입력(사용 스킬 포함)·경기 로그(수정/삭제), 주간 갱신, **거래 내역 로그**
1. **홈 / MMR 랭킹 TOP RATE** (공개 기본 화면) — 소속 팀·주 포지션 표시, 상단 리그 선택 드롭다운 (전 공개 페이지 공통)
2. **팀 순위표** (공개)
3. **경기 로그** — 목록/상세 (공개)
4. **선수 페이지** — MMR 추이·전적·티어 이력 (공개)
5. **관리자** — 경기 입력(선수 8명 + 결과), 경기 목록(수정/삭제), 선수 관리, 팀·로스터(감독/코치), 리그 관리(생성/보관)

## 8. 마일스톤

| 단계 | 범위 | 상태 |
|---|---|---|
| **M1 — 간소화 MVP** | 선수/팀(감독·코치)/로스터, 승무패 경기 입력, MMR 엔진(±30·언더독·재계산), 랭킹/팀 순위/경기 로그/선수 페이지(MMR 추이) | rev.4 기준 구현 완료 → **간소화 개조 필요** (스탯 입력·육각형 제거, 결과 선택식 입력) |
| **M1.5 — 리그 생성기** (rev.6 신규) | **전역 유저 풀 + 리그 참가 분리**(user/league_player + 기존 DB 마이그레이션), **리그 목록·셋업 체크리스트 허브**(기간 진행 중 수정), **티어 배치 보드**(드래그 배치 + 티어별 급여/MMR/정원 설정), **스킬 마스터 시드 + 스킬 티어 배치 보드**(드래그, 이모지/PNG 아이콘), **경매 드래프트**(라운드별 코인·랜덤 등장·낙찰 입력·되돌리기), 팀 편성 보드(십자 4분할 드래그), **팀 코인 관리(지급/차감·이력)**, **팀 스킬 소유 등록·표시**, **스킬 구매(티어별 가격·코인 차감)**, **트레이드(선수 교환±코인)**, **공개 팀 순위(로스터·스킬 노출)**, 리그 선택 드롭다운, 리그 관리(생성/보관), 시드 데이터 제거 | 예정 |
| **M2** | 월요일 정기 갱신 — **MMR 순위 기반 티어 재배치**(§3.3), 휴면 강등, 승격 보상, 아마추어 팀 배정 도우미(1·3·5·8 vs 2·4·6·7) | 예정 |
| **로드맵** | 외부 공개 배포(도메인+HTTPS), 유저 닉네임·리그 통산 이력 연동(유저 페이지), 필요 시 §9 기능 부활 | — |

## 9-1. 기획안에 있으나 아직 시스템에 없는 규칙 (2026-07-25 대조)

기획안 1.01·1.02에서 추가됐지만 시스템이 다루지 않는 항목. **누락이 아니라 의도적 보류**이며,
운영은 관리자가 기획안을 보고 수동 판단한다. 필요해지면 아래 순서로 착수.

| 기획안 | 규칙 | 현재 대응 | 비고 |
|---|---|---|---|
| 5장 | 벤/리로드 게이지 **자동 지급**(주간 순위 1위 1개~4위 4개, 카페 보상) | 관리자 수동 지급 | 주간 갱신(M2)에 얹는 것이 자연스러움 |
| 4-3 | 벤/리로드 **사용 기록**(경기당 1회·하루 3회, 게이지 5 차감) | 미구현 | 경기 입력 폼 확장 필요 |
| 6-1 | **임대** — 임대팀 소속으로 출전, 급여 적용, **MMR 미적용**, 무능력만 | 미구현 | MMR 엔진에 "이 출전은 점수 미반영" 개념이 없음 |
| 6-2 | 트레이드 **선수당 3회 제한** | 무제한 | `roster_log`로 횟수 산출은 가능 |
| 4-1 | **주간 통합 4위 2회 → 급여 상한 +5** | 경기 입력의 팀별 보너스 입력에 수동 반영 | 주간 갱신(M2) 대상 |
| 9-2·10 | **인시즌 토너먼트 / 플레이오프** (출전 6회·12회 이상 조건, 다전제) | 경기 유형에 없음(리그/아마추어/프로만) | 유형 추가 + 출전 횟수 검증 |
| 11장 | **징계** — 경고 누적 1/2/3회 → 출장 정지·영구 밴 | 미구현 | |
| 7장 | **이주의 선수** 투표 | 미구현 | |

## 9. 제외 기능 (rev.5에서 삭제 — 상세 스펙은 `PRD-rev4.md` 보관본 참조)

| 기능 | 제외 사유 |
|---|---|
| 개인 스탯 8종 입력 (골·어시·터치·패스·수비·볼경합·턴오버·활동량), MoM·수비왕 | 입력 부담 — 간소화의 직접 대상 |
| 육각형 스텟 차트, 선수 기록표(스탯 누적·평균) | 스탯 입력 없이는 불가 |
| 구 Elo 레이팅 병행 표시 | MMR 단일 지표로 통일 |
| 득점(스코어) 입력, 골 합계 검증, 급여 초과 골 패널티 자동 반영 | 결과만 입력 — 골 패널티는 게임 내 규칙으로 유지 |
| 스킬 통계(픽 수·승률·평균 스탯) | 로드맵으로 이동. ※ rev.6에서 포함으로 변경: 스킬 마스터(54종)·리그별 스킬 티어·팀 스킬 소유, **경기별 사용 스킬 입력(소유 스킬만) + 당일 재사용(피어리스룰) 경고** |
| 엑셀 `.xlsm` 과거 기록 임포트 | 스탯 중심 데이터 — 신규 시즌은 티어 배치로 시작 |

## 부록 A — 티어 배치 참고 예시 (초축인티어표.txt, 총 45명)

> **rev.6부터 시드 데이터 아님.** 시스템은 빈 상태로 시작하며, 아래 명단은 관리자가
> 마법사 3단계(선수 등록)에서 붙여넣기로 입력할 때 쓸 수 있는 참고 자료로만 보존한다.

| 티어 | 초기 MMR | 급여 | 선수 (인원) |
|---|---|---|---|
| S | 3300 | 30 | 릴리, 쿼스크, 말환, 갈비, 버질, 햄식, 올피, 소금빵 (8 — 정원 마감) |
| A+ | 3100 | 25 | 피를로, 아지, 창모, 플메 (4) |
| A | 3000 | 25 | 정이, 도렌누, 조율 (3) |
| A- | 2900 | 25 | 스노윙 (1) |
| B+ | 2700 | 20 | 수아, 다홍매, 라이브, 엑고, 흐뭄, 팡질 (6 — 정원 마감) |
| B | 2600 | 20 | 치킨, 바운드, 응깃, 배드윌, 티벗, 쥬지 (6 — 정원 마감) |
| B- | 2500 | 20 | 한동숙, 삼식, 느타리, 민수 (4) |
| C+ | 2300 | 15 | 쵸비, 던파, 킬비, 광어, 캬아아, 오메가 (6 — 정원 마감) |
| C | 2200 | 15 | 노, 일섹, 고래 (3) |
| C- | 2100 | 15 | 민물고기, 구드론, 다스, 시아 (4) |

## 부록 B — 스킬 티어표 (스킬 티어 배치 보드의 리그별 기본값)

| 티어 | 팀 소유 상한 | 스킬 (종수) |
|---|---|---|
| S | 2개 | 유체이탈, 점멸, 염력, 쇄도, 시즈모드, 핵폭발, 웹, 저격 (8) |
| A | 4개 | 부스터, 표식, 골리앗, 쇠약, 강타, 쐐기벌레, 장애물, 그물, 시한폭탄, 비행, 포탈, 울트라, 저장, 질주, 킥력, 돌격 (16) |
| B | 4개 | 봉인, 토템, 충격탄, 은신, 자석, 깃털, 백스텝, 와이어, 압정, 워프, 셔틀, 디펜시브, 늪, 위성, 촉수 (15) |
| C | 4개 | 귀환, 포톤캐논, 잠복, 일섬, 피지컬, 집중, 왜곡, 감아차기, 마취, 점령, 빙의, 목줄, 주력, 끈기, 야마토건 (15) |

> 팀 소유 상한은 기획안 3-3·4-3 기준(S2/A4/B4/C4). 팀당 총 보유는 13개(§3.1).
